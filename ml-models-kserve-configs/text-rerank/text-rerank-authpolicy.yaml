apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: text-rerank-rule-policy
  namespace: istio-system
spec:
  action: ALLOW
  rules:
  - from:
    - source:
        ipBlocks:
        - "*"
    to:
    - operation:
        hosts:
        - text-rerank.dev.curieo.org
  selector:
    matchLabels:
      app: istio-ingressgateway
